
package usandoarraylist;

/**
 *
 * @author Fabinhu
 */
public class Anotacoes {
    
    /*
    https://receitasdecodigo.com.br/java/exemplo-de-foreach-do-java-8
    https://blog.betrybe.com/java-foreach/
    */
    
    /*Criando o ArrayList    
    ArrayList< Qualquer_Tipo > nomeDoArrayList = new ArrayList< Qualquer_Tipo >();    
    */
    
    /*Passando os Alunos e Notas
    ArrayList<tipoArray> nomeObjeto = new ArrayList<ipoArray>()
    nomeObjeto.Add("Conteudo");
 */
    
     /*
    Exemplo 1 - Consultar posição por noem  usar:
    int index = NotaAluno.indexOf("Marcos");
    System.out.println(index);
    --> Retorno: 1 (ou seja posição 1
    
    Exemplo 2 - Verificar se na Lista contém determinado nome
    System.out.println(NotaAluno.contains("Maria"));
    --> Retorno: True (indicando que possui dentro da lista passada
    
    Criar Vetor de Dois indices
    int iAluno = 0, iNota = 0;
	double dMaior = dArrayBid2[0][0];
	
	for (iAluno = 0; iAluno < dArrayBid2.length; iAluno++) {
		for (iNota = 0; iNota < dArrayBid2[iAluno].length; iNota++) { 
			
			if(dArrayBid2[iAluno][iNota] > dMaior){
			dMaior = dArrayBid2[iAluno][iNota]; 
			}
	}

		sMaior = String.format("\n %s %.2f ","A maior Nota é",dMaior);

}--------------------------------------------------
    
      Menor e maior Valor
      int soma = 0;
    int menor = v[0]; // v[0] = 1o. valor armazenador no vetor "v"
    int maior = v[0];
    for (i=0; i<n; i++) {
      soma = soma + v[i];

      if (v[i] < menor)
         menor = v[i];

      if (v[i] > maior)
         maior = v[i];
    }
    */
    
    
    
  /*
  while(!resposta.equals(true) ){
  //Testando comparação
  System.out.println(RespostaArray);
      
  System.out.println("Digite Quantas possições Deseja? \n Valores Entre 0 - 10");
  ValorPosicoesArray=teclado.nextInt();
    
 String[] NotaAluno = new String[ValorPosicoesArray];
 
    NotaAluno[0]=("Juca");
    NotaAluno[1]=("Marcos");
    NotaAluno[2]=("Maria");
    NotaAluno[3]=("Bazan");
    NotaAluno[4]=("Bruna");
    NotaAluno[5]=("Pedro");
    NotaAluno[6]=("Julia");
    NotaAluno[7]=("Vanessa");
    NotaAluno[8]=("Robs");
    NotaAluno[9]=("Simone");
  }
    for(int contador=0;contador < NotaAluno; contador++)
    System.out.println(NotaAluno.get(contador));
    
    teclado.close();
    }
    }
public void ArrayNotaAluno(){
    
    
    }
    */
    
}
